"""Enterprise-only AutoEvalBench helpers.

The implementation lives in the private `ninai-enterprise` repo/package.
This stub remains only to make accidental imports fail loudly.
"""


raise ImportError(
    "AutoEvalBench is an enterprise feature. Install the private 'ninai-enterprise' package to use it."
)
